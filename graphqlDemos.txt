mutation {
    registerUser(input: {
        name: "name",
        email: "a@bcd.com",
        password: "password"
    }) {
        id
        name
        email
    }
}

query {
    users {
        name
        email
    }
}